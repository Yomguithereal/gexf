/**
 * Gexf Writer Unit Tests
 * =======================
 *
 * Testing the writing utilities of the gexf library.
 */

if (!('window' in this)) {
  var assert = require('assert'),
      gexf = require('../../index.js'),
      helpers = require('../helpers.js'),
      async = require('async');
}

describe('Writer', function() {

});
